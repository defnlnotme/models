[project]
name = "models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.12.0",
    "auto-round>=0.9.7",
    "openai>=2.16.0",
    "optimum[nncf,openvino]>=2.1.0",
]
