services:

  openarc:
    # image: ghcr.io/searchsavior/openarc:latest
    image: ghcr.io/searchsavior/openarc-battlemage:latest 
    container_name: openarc

    # GPU access
    devices:
      - /dev/dri:/dev/dri
      - /dev/accel:/dev/accel
    
    # Volumes
    volumes:
      - ${MODEL_PATH:-~/data/models/ov}:/models
      - openarc-config:/persist
    
    # Ports
    ports:
      - "8000:8000"
    
    # Environment variables
    environment:
      - OPENARC_API_KEY=${OPENARC_API_KEY:-openarc-default-key}
      - OPENARC_AUTOLOAD_MODEL=${OPENARC_AUTOLOAD_MODEL:-}
      - OPENARC_MODELS_DIR=${OPENARC_MODELS_DIR:-/models}
      # Intel GPU tuning variables
      - NEOReadDebugKeys=1
      - OverrideGpuAddressSpace=48
      - EnableImplicitScaling=1
    
    # Restart policy
    restart: unless-stopped

    security_opt:
      - label=disable

    network_mode: host
    
    # Logging (optional)
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  openarc-config:
